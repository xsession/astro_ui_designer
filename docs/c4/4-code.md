# C4 C4 — code/module map

Canonical standalone implementations live under `standalone/js/`. The VS Code `designer/` copy mirrors the browser-facing code. Host code lives in `launch-designer.mjs`, `workspace-tools.mjs` and `vscode-extension/`. Agent code lives in `integrations/hermes/`.

Use [Module guide](../contributor/MODULE_GUIDE.md) for ownership and extension rules. Generated module-level lists should be regenerated when files are added or renamed.
