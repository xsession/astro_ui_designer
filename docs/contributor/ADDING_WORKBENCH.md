# Adding a workbench

Register a unique dock key, label and origin zone; implement a real renderer; add a panel command so it is searchable/hotkey-assignable; add a semantic tooltip; ensure left/right/bottom/floating relocation works; add tab-functionality and dock tests. Avoid adding a permanent top-toolbar button unless the action is high-frequency.
