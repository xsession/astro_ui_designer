# Design-system model

Tokens provide semantic reusable values, components provide structural reuse, variants provide controlled dimensions of variation, and libraries provide project-to-project distribution. Instances should inherit from a source component while retaining narrow overrides. This mirrors code architecture more closely than copying detached visual groups and improves both source generation and usage analysis.
