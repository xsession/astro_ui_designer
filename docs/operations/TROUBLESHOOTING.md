# Operations troubleshooting

Start from the Console and Problems panels, then reproduce the failing host call independently. For workspace operations verify the selected root and path containment. For preview verify the target project's package/runtime dependencies. For Git verify repository state and identity/permissions outside the designer. For MCP inspect stderr without contaminating stdio stdout. For stale round-trip plans reload the source and create a new plan.
