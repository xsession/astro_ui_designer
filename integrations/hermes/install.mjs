import fs from 'node:fs';import path from 'node:path';import { fileURLToPath } from 'node:url';
const here=path.dirname(fileURLToPath(import.meta.url)),root=path.resolve(process.argv[2]||process.cwd()),src=path.join(here,'skill-src','astro-ui-designer'),dst=path.join(root,'.hermes','skills','astro-ui-designer');fs.mkdirSync(dst,{recursive:true});for(const n of fs.readdirSync(src))fs.copyFileSync(path.join(src,n),path.join(dst,n));console.log(`Installed Astro UI Designer skill to ${dst}`);
