import fs from 'node:fs';import path from 'node:path';import { createSampleProject } from '../standalone/js/model.js';import { generateAstroProject } from '../standalone/js/astro-exporter.js';const p=createSampleProject(),out=path.resolve('examples/generated-astro');fs.rmSync(out,{recursive:true,force:true});for(const [rel,text] of Object.entries(generateAstroProject(p))){const f=path.join(out,rel);fs.mkdirSync(path.dirname(f),{recursive:true});fs.writeFileSync(f,text)}console.log(`Generated ${Object.keys(generateAstroProject(p)).length} files in examples/generated-astro`);
