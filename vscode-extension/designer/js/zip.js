function crc32(data){let c=-1;for(const b of data){c^=b;for(let k=0;k<8;k++)c=(c>>>1)^((c&1)?0xedb88320:0)}return (c^-1)>>>0}
const u16=n=>new Uint8Array([n&255,(n>>>8)&255]),u32=n=>new Uint8Array([n&255,(n>>>8)&255,(n>>>16)&255,(n>>>24)&255]);
function join(chunks){const n=chunks.reduce((a,b)=>a+b.length,0),out=new Uint8Array(n);let p=0;for(const c of chunks){out.set(c,p);p+=c.length}return out}
export function makeZip(files){const enc=new TextEncoder(),locals=[],central=[];let offset=0;for(const [name,text] of Object.entries(files)){const nb=enc.encode(name),data=enc.encode(String(text)),crc=crc32(data),local=join([u32(0x04034b50),u16(20),u16(0),u16(0),u16(0),u16(0),u32(crc),u32(data.length),u32(data.length),u16(nb.length),u16(0),nb,data]);locals.push(local);central.push(join([u32(0x02014b50),u16(20),u16(20),u16(0),u16(0),u16(0),u16(0),u32(crc),u32(data.length),u32(data.length),u16(nb.length),u16(0),u16(0),u16(0),u16(0),u32(0),u32(offset),nb]));offset+=local.length}const cdir=join(central),body=join(locals),end=join([u32(0x06054b50),u16(0),u16(0),u16(central.length),u16(central.length),u32(cdir.length),u32(body.length),u16(0)]);return new Blob([body,cdir,end],{type:'application/zip'})}
export function downloadZip(files,name='astro-ui-project.zip'){const url=URL.createObjectURL(makeZip(files)),a=document.createElement('a');a.href=url;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000)}
