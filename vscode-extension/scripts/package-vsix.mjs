import fs from 'node:fs';import path from 'node:path';import { fileURLToPath } from 'node:url';import { spawnSync } from 'node:child_process';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'),dist=path.join(root,'dist');fs.mkdirSync(dist,{recursive:true});const pkg=JSON.parse(fs.readFileSync(path.join(root,'package.json'),'utf8')),name=`astro-ui-designer-vscode-${pkg.version}.vsix`,out=path.join(dist,name),tmp=fs.mkdtempSync('/tmp/aui-vsix-');const ext=path.join(tmp,'extension');fs.cpSync(root,ext,{recursive:true,filter:s=>!s.includes(`${path.sep}dist${path.sep}`)&&!s.includes(`${path.sep}.git${path.sep}`)});const content=`<?xml version="1.0" encoding="utf-8"?><Types xmlns="http://schemas.openxmlformats.org/package/2006/content-types"><Default Extension="json" ContentType="application/json"/><Default Extension="js" ContentType="application/javascript"/><Default Extension="html" ContentType="text/html"/><Default Extension="css" ContentType="text/css"/><Default Extension="svg" ContentType="image/svg+xml"/></Types>`;fs.writeFileSync(path.join(tmp,'[Content_Types].xml'),content);const r=spawnSync('zip',['-qr',out,'.'],{cwd:tmp});fs.rmSync(tmp,{recursive:true,force:true});if(r.status!==0)throw new Error('zip failed');console.log(out);
